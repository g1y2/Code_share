#include<iostream>
using namespace std;

int main()
{
	int a[100], temp;
	for (int i = 0; i < 100; i++)
	{
		cin >> a[i];
		if (a[i] == 0)
		{
			temp = i;
			break;
		}
	}
	for (int i = temp - 1; i >= 0; i--)
		cout << a[i] << ' ';
	return 0;
}
